import { TableColumn } from 'shared/types'

// const ActionButtons = observer(() => {
//   return <></>
// })

export const getColumns = (): TableColumn[] => [
  {
    key: 'id',
    title: 'id',
  },
]
